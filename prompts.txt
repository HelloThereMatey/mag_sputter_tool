See my launch_... bash script. That is run via a DESKTOP icon on my RPi5 desktop. I want to specifiy the size & location of each window that is launched by the script. Window 1 is the GUI window and defined by the .ui file and app.py & the other window is the terminal. I want GUI window to open on the small screen "HDMI-A-2" and the terminal on screen: "HDMI-A-1". I want the GUI tp expand to fill the small screen, as 